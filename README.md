# 20 bài tập Dart

Gồm 20 bài tập Dart cơ bản và giao diện Flutter để nhập dữ liệu, chạy bài và xem kết quả dễ dàng hơn so với terminal.

## Chạy giao diện

```powershell
cd flutter_application_bt
flutter pub get
flutter run -d chrome
```

Các bài riêng nằm trong `bai_01.dart` đến `bai_20.dart`.
